/** @type {import('next').NextConfig} */
const nextConfig = {
    output:  'standalone'
    // plugins: [
    //   "postcss-import",
    //   "tailwindcss",
    //   "autoprefixer"
    // ]
};

export default nextConfig;
